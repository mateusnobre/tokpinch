# Normalise line endings to LF in the repository; check out as LF on all platforms.
* text=auto eol=lf

# Binaries — no line-ending conversion
*.db      binary
*.db-shm  binary
*.db-wal  binary
*.png     binary
*.jpg     binary
*.ico     binary
